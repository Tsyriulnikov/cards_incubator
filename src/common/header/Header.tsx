import React from 'react';
import {Navigate} from "react-router-dom";
import {Nav} from "./Navigation/Nav";

export const Header = () => {
    return (
        <div>
            <Nav/>
        </div>
    );
};

